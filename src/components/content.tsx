import {Container} from "@mui/material";
import { TodoPage } from "features/todo";

const Content = () => {
  return (
    <Container>
      <TodoPage/>
    </Container>
  );
};

export default Content;